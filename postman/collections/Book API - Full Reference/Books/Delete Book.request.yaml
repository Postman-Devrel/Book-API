$kind: http-request
name: Delete Book
description: 'Deletes a book by ID. Returns 404 if the book is not found.'
method: DELETE
url: '{{baseUrl}}/api/v1/books/:id'
order: 5000
pathVariables:
  - key: id
    value: '1'
